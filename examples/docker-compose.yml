version: '2.3'

volumes:
  etestatic:

services:
  etesync:
    container_name: etesync
    hostname: etesync
    image: etesync:latest
    expose:
      - 8000    
    volumes:
      - etestatic:/var/www/static:rw
    environment:
      - DEBUG=True
      - SERVER=uwsgi
      - SUPER_USER=admin
      - SUPER_PASS=admin
    restart: on-failure
    network_mode: "bridge"
  nginx:
    container_name: nginx
    hostname: epoch
    image: nginx
    ports:
      - 443:443
      - 80:80
    volumes:      
      - ./nginx-example.conf:/etc/nginx/conf.d/default.conf:ro      
      - etestatic:/var/www/static:ro
    links:
      - etesync
    #depends_on:
    #  - etesync
    restart: on-failure
    network_mode: "bridge"
